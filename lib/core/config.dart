/// Small runtime config constants for image upload/resizing.
class AppConfig {
  // Max dimension (width) for avatar resize. Change as needed.
  static const int avatarMaxDimension = 1024;

  // JPG quality for resized images (0-100).
  static const int avatarJpgQuality = 85;
}

